[build]
builder = "RAILPACK"

[deploy]
startCommand = "python hw3/app.py --host 0.0.0.0"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
